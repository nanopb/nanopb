# Test enum to string functionality

Import('env')


env.Append(CPPPATH = ["#../nanogrpc", '$NANOGRPC_COMMON'])
p = env.Program(["unit.c", "$COMMON/pb_encode.o", "$COMMON/pb_decode.o", "$COMMON/pb_common.o", "$NANOGRPC_COMMON/nanogrpc.pb.o"])
env.RunTest(p)
print('bleee')
# raise
