syntax = "proto2";
import 'nanopb/options.proto';

message SubMessage {
    repeated int32 foo = 1 [(nanopb.fieldopt).type = FT_POINTER];
}

message MainMessage {
    repeated SubMessage bar = 1 [(nanopb.fieldopt).max_count = 5];
}

